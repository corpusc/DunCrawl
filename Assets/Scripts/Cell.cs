using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class Cell {
	//public List<PicData> Pics = new List<Texture>(); // can have one of every type (except floor and wall are mutex)
}
